{
    "name": "Resample",
    "function": "pop_resample",
    "type": "process",
    "suffix": "RS",
    "category": "Tools",
    "description": "Resample the dataset to a new frequency.",
    "github_url": "https://github.com/sccn/eeglab/blob/master/functions/popfunc/pop_resample.m",
    "inputs": [
        {
            "name": "EEG",
            "type": "dataset",
            "arg_type": "positional",
            "description": "Input EEG structure",
            "required": true
        },
        {
            "name": "freq",
            "type": "float",
            "arg_type": "positional",
            "description": "New sampling rate (Hz) (e.g. 250).",
            "required": true,
            "default": 250
        },
        {
            "name": "options",
            "type": "key_value_list",
            "description": "Additional resampling options",
            "required": false,
            "allowed_keys": {
                "fc": "Anti-aliasing filter cutoff (pi rad / sample) {default 0.9}",
                "df": "Anti-aliasing filter transition band width {default 0.2}"
            },
            "default": []
        }
    ],
    "outputs": [
        {
            "name": "EEG",
            "type": "dataset",
            "description": "Resampled EEG structure"
        }
    ],
    "help_text": "pop_resample() - resample dataset (pop up window).\n\nUsage:\n  >> [OUTEEG] = pop_resample( INEEG ); % pop up interactive window\n  >> [OUTEEG] = pop_resample( INEEG, freq);\n  >> [OUTEEG] = pop_resample( INEEG, freq, fc, df);\n\nGraphical interface:\n  The edit box entitled \"New sampling rate\" contains the frequency in\n  Hz for resampling the data. Entering a value in this box  is the same \n  as providing it in the 'freq' input from the command line.\n\nInputs:\n  INEEG      - input dataset\n  freq       - frequency to resample (Hz)  \n\nOptional inputs:\n  fc         - anti-aliasing filter cutoff (pi rad / sample)\n               {default 0.9}\n  df         - anti-aliasing filter transition band width (pi rad /\n               sample) {default 0.2}\n\nOutputs:\n  OUTEEG     - output dataset\n\nAuthor: Arnaud Delorme, CNL/Salk Institute, 2001\n\nNote: uses the resample() function from the signal processing toolbox\n      if present. Otherwise use griddata interpolation method (it should be\n      reprogrammed using spline interpolation for speed up).\n\nSee also: resample(), eeglab()"
}
